// layout.js
import { SplashProvider } from "./context/splashcontext";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import Splashscreen from "./components/splashscreen";
import Bg from "./components/bg.js";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "Youssef El Rhomari | Portfolio",
  description: "Generated by create next app",
  icons: {
    icon: "/favicon.png",
  },
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased `}
      >
        <Splashscreen />
        <main className="relative border-2 bg-stone-100 border-black m-5 min-h-[calc(100vh-40px)]">
          <div>
            <SplashProvider>
            {children}
            <div className=" fixed bottom-0 ml-10 md:text-[15px] text-[12px] text-black">&copy; Youssef elrhomari</div>
            </SplashProvider>
          </div>
        </main>
      </body>
    </html>
  );
}